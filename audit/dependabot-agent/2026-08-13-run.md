# Dependabot Agent — Next-Gen-Reco — Run 2026-08-13

Scope: `themanoj-025/Next-Gen-Reco`, default branch `main`.
Identity: `dependabot-agent[bot]` (repo admin token). Merge method: squash.

## Baseline CI was broken — fixed first (Phase 0)
`docker` job failed at the Trivy image scan (exit 1): the image still contained unpatched copies of `msgpack 1.1.2` (GHSA-6v7p-g79w-8964, fix 1.2.1) and `setuptools 70.3.0` (CVE-2025-47273, fix 78.1.1). The deps-stage pip upgrade installs patched versions in /usr/local, but `python:3.12-slim` also ships unpatched apt copies in `/usr/lib/python3/dist-packages` that Trivy scans.
Fix: purge the apt copies (`apt-get purge python3-msgpack python3-setuptools`) — mirroring the approach already applied in Price-My-Car. Commit: `be146cd` (pushed to main).

## PR inventory (7 open, all pip)
#18 streamlit, #17 pandas, #13 pyarrow, #11 numpy, #7 scikit-learn, #3 requests, #2 joblib

## Actions taken
- Merged `main` (with the Dockerfile fix) into all 7 PR branches + pushed (CI re-triggered).

## Status
Merged: 0 (waiting on CI). Held: 0. Waiting CI: 7.

## Platform note (anomaly)
Workflow runs stalled in the GitHub-hosted runner queue starting ~12:43 UTC (0 billable time, no runner assigned). Known recurring GitHub Actions scheduling issue; expected to self-resolve. PRs with green CI should be merged once runs complete.
