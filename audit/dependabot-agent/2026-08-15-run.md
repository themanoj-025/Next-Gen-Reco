# Next-Gen-Reco — Dependabot Agent Audit (2026-08-15)

**Repo:** themanoj-025/Next-Gen-Reco · **Ecosystems:** pip, pre-commit
**CI:** py_compile all files, core-module import checks, UI syntax parse, data-file checks, pytest (exit-5 tolerated)

## Verdicts
| PR | Change | Type | Verdict |
|----|--------|------|---------|
| #1 | pandas >=1.5.0 → >=3.0.5 | major | MERGE-WITH-CAUTION (verified) — pending CI |
| #2 | scikit-learn >=1.3.0 → >=1.9.0 | major | MERGE-WITH-CAUTION (verified) — pending CI |
| #3 | joblib >=1.2.0 → >=1.5.3 | major | MERGE-WITH-CAUTION (verified) — pending CI |
| #4 | numpy >=1.24.0 → >=2.5.2 | major | MERGE-WITH-CAUTION (verified) — pending CI |
| #5 | pyarrow >=10.0.0 → >=25.0.1 | major | MERGE-WITH-CAUTION (verified) — pending CI |
| #6 | streamlit >=1.60.0 → >=1.61.1 | minor | AUTO-MERGE-eligible (verified) — pending CI |
| #7 | requests >=2.28.0 → >=2.34.2 | minor | AUTO-MERGE-eligible (verified) — pending CI |
| #8/#9 | pre-commit-hooks v5→6, black 25.9→26.5.1 | pre-commit | verify-on-CI |

## Verification (Phase 2.3)
- Clean venv install resolved to pandas 3.0.5, numpy 2.5.2, scikit-learn 1.9.0, joblib 1.5.3, streamlit 1.61.1, requests 2.34.2 (no resolution errors; pyarrow resolved to 24.0.0 on this platform — no cp314 Windows wheel for 25.x; CI on Linux py3.12 will resolve 25.0.1).
- CI-equivalent import checks ALL pass: `app._paths`, `app.model` (load_movies/load_tags/load_model/predict_rating), `app.enrichment` (NDEnrichment), `app.recommender` (MovieRecommender).
- py_compile: all .py files compile.
- Runtime smoke: load_movies() loaded the 2M-row dataset and predict_rating executed real pandas ops under pandas 3.0.5 — no API-compat errors. A full prediction could not complete locally due to the machine paging-file limit (OpenBLAS/numpy allocation failure) — documented env limitation, not a code break.

## Notes
- Heavy ML-stack majors verified compatible with the app's code (imports + real data ops). CI (Linux) will exercise the full path once runners recover.
