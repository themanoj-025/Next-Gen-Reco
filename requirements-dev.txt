# Dev-only dependencies for CI (validate job) and local test runs.
# Not needed on Streamlit Cloud — runtime deps live in requirements.txt.
# Mirrors the "Dev-only" section of requirements.txt.

httpx2>=0.28.1
nbformat>=5.10.0
pytest>=8.0.0
pytest-cov>=5.0.0
# pytest-asyncio>=1.0.0
