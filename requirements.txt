streamlit>=1.61.1
pandas>=3.0.5
numpy>=1.24.0
scikit-learn>=1.9.0
joblib>=1.2.0
plotly>=6.9.0
python-dotenv>=1.2.2
requests>=2.34.2
pyarrow>=10.0.0

# xgboost is optional — only needed for retraining the model.
# The pre-trained model uses RandomForest and doesn't require xgboost at runtime.
# xgboost>=1.7.0

# ── Dev-only (not needed on Streamlit Cloud) ──────────────────────────────────
# pytest>=7.0.0
# pytest-cov>=4.0.0
